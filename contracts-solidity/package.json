{
  "name": "bitshield-solidity",
  "version": "1.0.0",
  "description": "BitShield Solidity contracts for Arbitrum deployment",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network arbitrumSepolia",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.5"
  }
}
